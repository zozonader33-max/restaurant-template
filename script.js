let english=false;const $$=s=>document.querySelectorAll(s),$=s=>document.querySelector(s);
const setLanguage=()=>{english=!english;document.documentElement.lang=english?'en':'ar';document.documentElement.dir=english?'ltr':'rtl';$$('[data-ar]').forEach(el=>el.innerHTML=el.dataset[english?'en':'ar']);$$('[data-ar-placeholder]').forEach(el=>el.placeholder=el.dataset[english?'en-placeholder':'ar-placeholder']);$$('.lang').forEach(b=>b.textContent=english?'ع':'EN');document.title=english?'Sinna | Dental Studio':'سِنّة | عيادة أسنان'};
$$('.lang').forEach(b=>b.onclick=setLanguage);
let selectedService=$('.service.active'),selectedSlot=$('.slot.active');
$$('.service').forEach(b=>b.onclick=()=>{$$('.service').forEach(x=>x.classList.remove('active'));b.classList.add('active');selectedService=b});
$$('.slot').forEach(b=>b.onclick=()=>{$$('.slot').forEach(x=>x.classList.remove('active'));b.classList.add('active');selectedSlot=b});
$('#booking-form').addEventListener('submit',e=>{e.preventDefault();const name=$('#client-name').value;const service=selectedService.dataset[english?'serviceEn':'service'];const day=selectedSlot.dataset[english?'dayEn':'day'];const time=selectedSlot.querySelector('b').textContent;$('#toast-copy').textContent=english?('Thanks '+name+' — '+service+' / '+day+' '+time):('شكراً '+name+' — '+service+' / '+day+' '+time);$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),4500)});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});$$('.reveal').forEach(el=>observer.observe(el));
function tooth(){const host=$('#tooth-scene');if(!window.THREE||!host)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(32,host.clientWidth/host.clientHeight,.1,100);camera.position.set(0,.2,7);const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(host.clientWidth,host.clientHeight);renderer.setClearColor(0x000000,0);host.appendChild(renderer.domElement);const root=new THREE.Group();scene.add(root);const mat=new THREE.MeshPhysicalMaterial({color:0xfaf8f0,roughness:.2,metalness:0,clearcoat:.55,clearcoatRoughness:.18});const edge=new THREE.MeshPhysicalMaterial({color:0xe6f4ff,roughness:.28,clearcoat:.45});const make=(geo,x,y,z,sx,sy,sz)=>{const m=new THREE.Mesh(geo,mat);m.position.set(x,y,z);m.scale.set(sx,sy,sz);root.add(m);return m};const sphere=new THREE.SphereGeometry(1,48,32);make(sphere,0,.48,0,.92,1.1,.7);make(sphere,-.48,.72,.02,.54,.62,.63);make(sphere,.48,.72,.02,.54,.62,.63);const rootGeo=new THREE.CapsuleGeometry(.28,.95,10,30);make(rootGeo,-.31,-.96,.02,.8,1,.65);make(rootGeo,.31,-.96,.02,.8,1,.65);const shine=new THREE.Mesh(new THREE.SphereGeometry(.43,32,24),edge);shine.position.set(-.3,.83,.52);shine.scale.set(.7,.9,.18);root.add(shine);root.rotation.set(.12,-.32,0);scene.add(new THREE.HemisphereLight(0xffffff,0x1559d6,2.3));const key=new THREE.DirectionalLight(0xffffff,3);key.position.set(-3,4,5);scene.add(key);const rim=new THREE.PointLight(0xff684f,3,20);rim.position.set(3,-1,3);scene.add(rim);let tx=0,ty=0;host.addEventListener('pointermove',e=>{const r=host.getBoundingClientRect();tx=((e.clientX-r.left)/r.width-.5)*.7;ty=((e.clientY-r.top)/r.height-.5)*.35});host.addEventListener('pointerleave',()=>{tx=0;ty=0});function loop(t){root.rotation.y+=(tx-root.rotation.y)*.035;root.rotation.x=.12+(ty-root.rotation.x)*.035;root.position.y=Math.sin(t*.0012)*.09;renderer.render(scene,camera);requestAnimationFrame(loop)}loop(0);addEventListener('resize',()=>{camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix();renderer.setSize(host.clientWidth,host.clientHeight)})}tooth();