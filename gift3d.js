import * as THREE from "https://unpkg.com/three@0.162.0/build/three.module.js";
const host=document.querySelector("#gift3d"),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(34,1,.1,100),renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});camera.position.set(5,3.8,7);camera.lookAt(0,.35,0);renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.outputColorSpace=THREE.SRGBColorSpace;host.append(renderer.domElement);
const gift=new THREE.Group();scene.add(gift);const boxMat=new THREE.MeshPhysicalMaterial({color:0x9d405f,roughness:.36,metalness:.04,clearcoat:.2}),ribbonMat=new THREE.MeshPhysicalMaterial({color:0xf1b6bd,roughness:.25,metalness:.08,clearcoat:.55}),gold=new THREE.MeshStandardMaterial({color:0xe8c98f,metalness:.5,roughness:.25});
const box=new THREE.Mesh(new THREE.BoxGeometry(3.2,2.15,3.2),boxMat);box.position.y=-.05;gift.add(box);const lid=new THREE.Mesh(new THREE.BoxGeometry(3.45,.38,3.45),boxMat);lid.position.y=1.2;gift.add(lid);const rv=new THREE.Mesh(new THREE.BoxGeometry(.52,2.7,3.5),ribbonMat);rv.position.y=.18;gift.add(rv);const rh=new THREE.Mesh(new THREE.BoxGeometry(3.5,2.7,.52),ribbonMat);rh.position.y=.18;gift.add(rh);
const bow=new THREE.Group();for(const side of[-1,1]){const loop=new THREE.Mesh(new THREE.TorusGeometry(.67,.18,18,48,Math.PI*1.65),ribbonMat);loop.rotation.set(Math.PI/2,side*.55,side*.55);loop.position.set(side*.52,1.73,0);bow.add(loop)}const knot=new THREE.Mesh(new THREE.SphereGeometry(.3,32,20),gold);knot.position.y=1.68;bow.add(knot);gift.add(bow);
scene.add(new THREE.HemisphereLight(0xfff7ed,0x58273c,2.4));const key=new THREE.DirectionalLight(0xffffff,3);key.position.set(5,8,6);scene.add(key);const fill=new THREE.PointLight(0xf1b6bd,35,14);fill.position.set(-4,2,4);scene.add(fill);
let pointer={x:0,y:0};host.addEventListener("pointermove",e=>{const r=host.getBoundingClientRect();pointer.x=(e.clientX-r.left)/r.width-.5;pointer.y=(e.clientY-r.top)/r.height-.5});function resize(){renderer.setSize(host.clientWidth,host.clientHeight,false);camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix()}new ResizeObserver(resize).observe(host);resize();const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;function animate(t){gift.rotation.y=.45+(reduced?0:Math.sin(t*.00055)*.12+pointer.x*.45);gift.rotation.x=-.08+(reduced?0:pointer.y*.1);gift.position.y=reduced?-.1:Math.sin(t*.001)*.07-.1;renderer.render(scene,camera);requestAnimationFrame(animate)}requestAnimationFrame(animate);
